% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalizeGiotto_S3.R
\name{normalizeGiotto}
\alias{normalizeGiotto}
\alias{normalizeGiotto.GiottoDB}
\alias{normalizeGiotto.giotto}
\title{Normalize Expression for GiottoDB with dbMatrix support}
\usage{
normalizeGiotto(gobject, ...)

\method{normalizeGiotto}{GiottoDB}(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  expression_values = "raw",
  norm_methods = c("standard", "pearson_resid", "osmFISH", "quantile"),
  library_size_norm = TRUE,
  scalefactor = 6000,
  log_norm = TRUE,
  log_offset = 1,
  logbase = 2,
  scale_feats = TRUE,
  scale_genes = lifecycle::deprecated(),
  scale_cells = TRUE,
  scale_order = c("first_feats", "first_cells"),
  theta = 100,
  name = "scaled",
  update_slot = lifecycle::deprecated(),
  verbose = TRUE,
  ...
)

\method{normalizeGiotto}{giotto}(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  expression_values = "raw",
  norm_methods = c("standard", "pearson_resid", "osmFISH", "quantile"),
  library_size_norm = TRUE,
  scalefactor = 6000,
  log_norm = TRUE,
  log_offset = 1,
  logbase = 2,
  scale_feats = TRUE,
  scale_genes = lifecycle::deprecated(),
  scale_cells = TRUE,
  scale_order = c("first_feats", "first_cells"),
  theta = 100,
  name = "scaled",
  update_slot = lifecycle::deprecated(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{gobject}{A giotto or GiottoDB object}

\item{...}{Additional arguments passed to \code{Giotto::normalizeGiotto}}

\item{scale_feats}{Not supported for GiottoDB. Forced to \code{FALSE} with a
warning. Centering is performed inside \code{runPCA} via \code{db_svd}.}

\item{scale_cells}{Not supported for GiottoDB. Forced to \code{FALSE} with a
warning. Centering is performed inside \code{runPCA} via \code{db_svd}.}
}
\description{
For GiottoDB objects with dbMatrix expression, scaling (centering) is handled
implicitly during PCA via \code{db_svd}. If \code{scale_feats} or \code{scale_cells} are
\code{TRUE}, a warning is emitted and both are silently forced to \code{FALSE}. No
"scaled" expression slot is created.
}
\details{
S3 generic that dispatches to \code{Giotto::normalizeGiotto} with optimizations
for \code{\link[dbMatrix:dbMatrix]{dbMatrix::dbMatrix}}-backed expression data.
}
\concept{Expression processing}
