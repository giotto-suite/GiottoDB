% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processData_libraryNorm.R
\name{processData-dbMatrix-methods}
\alias{processData-dbMatrix-methods}
\title{Count Matrix Processing for GiottoDB Objects}
\usage{
processData(x, param, ...)
}
\arguments{
\item{x}{dbMatrix object}

\item{param}{S4 parameter class defining the transform operation. Can be:
\itemize{
\item \code{libraryNormParam} - library size normalization
\item \code{logNormParam} - log transformation
\item \code{osmFISHNormParam} - osmFISH normalization
\item \code{arcsinhNormParam} - arcsinh transformation
\item \code{l2NormParam} - L2/Euclidean normalization
\item \code{zscoreScaleParam} - z-score scaling
\item \code{defaultNormParam} - default normalization (library + log)
\item \code{list} - for chained operations
\item \code{tfidfNormParam} - TODO
\item \code{quantileNormParam} - TODO
\item \code{pearsonResidNormParam} - TODO
}}

\item{\dots}{additional params to pass to the underlying methods}
}
\value{
A dbMatrix object
}
\description{
processData contains methods for GiottoDB objects that provide various normalization
and scaling operations on count matrices as implemented in Giotto.

Working methods delegate to existing implementations in Giotto:
\itemize{
\item Library normalization (\code{libraryNormParam})
\item Log normalization (\code{logNormParam})
\item osmFISH normalization (\code{osmFISHNormParam})
\item Arcsinh transformation (\code{arcsinhNormParam})
\item L2 normalization (\code{l2NormParam})
\item Z-score scaling (\code{zscoreScaleParam})
\item Default normalization (\code{defaultNormParam}) - combines library + log
\item List processing - enables composable operations
}

Unsupported methods (TODO):
\itemize{
\item TF-IDF normalization
\item Quantile normalization
\item Pearson residuals normalization
}
}
\details{
All results are lazily evaluated. Please read \link[dbplyr:collapse.tbl_sql]{dbplyr::collapse.tbl_sql} to compute/save results to the db.
}
\examples{
\dontrun{
# Create a dbMatrix object
library(dbMatrix)
con <- DBI::dbConnect(duckdb::duckdb(), ":memory:")
mat <- matrix(rpois(100, 5), nrow = 10)
dbmat <- dbMatrix(mat, con = con, name = "test")

# Library normalization
lib_norm <- processData(dbmat, normParam("library"))

# Log normalization
log_norm <- processData(dbmat, normParam("log"))

# Chained operations
scaled <- processData(dbmat, list(
  normParam("library"),
  normParam("log"),
  scaleParam("zscore")
))
}
}
\seealso{
\code{\link[Giotto]{processData}} for the generic and other methods

\code{\link[Giotto]{processExpression}} for use with giotto objects

\code{\link[Giotto]{normParam}}, \code{\link[Giotto]{scaleParam}} for parameter creation
}
